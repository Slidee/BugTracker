<h1>Users List</h1>
<% if @users.empty? %>
  No Users found
<% else %>
  <%= will_paginate @users %>
  <table class="usersList tableList" summary="Users list">
    <thead>
      <tr class="trHead">
        <th>Infos</th>
        <th class="thActions">Actions</th>
      </tr>
    </thead>
    
    <tbody>
      <% @users.each do |user| %>
        <%= render user %>
      <% end %>
    </tbody>
  </table>
  <%= will_paginate @users %>

  <div class="listActions">
    <%= link_to "New user", new_user_path, :class => "new objectAction" %>
  </div>
<% end %>